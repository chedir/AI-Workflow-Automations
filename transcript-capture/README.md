# Transcript Capture

Fetches a video transcript and saves it as a date-stamped text file. Run once per video.

Transcripts are then grouped by week and source into a folder manually, for downstream analysis. Bulk/automated pulls tended to get rate-limited as bot traffic, so per-video is both more reliable and better curated.

Requirements and workflow specified by me; implementation AI-assisted.
